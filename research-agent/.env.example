# AI Gateway (Vercel OIDC after `eve link` + `vercel env pull`, or set AI_GATEWAY_API_KEY)
# AI_GATEWAY_API_KEY=

# Optional: default Notion parent page UUID for skills/schedules that publish notes
# (Share this page with the Vercel Connect Notion integration.)
# NOTION_PARENT_PAGE_ID=

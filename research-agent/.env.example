# AI Gateway (Vercel OIDC after `vercel link` + `vercel env pull`, or set AI_GATEWAY_API_KEY)
# AI_GATEWAY_API_KEY=

# Optional: default Notion parent page UUID for skills/schedules that publish notes.
# Setup: see README "Notion connection" (Connect MCP UID notion/notion, user auth).
# Share this page with the Vercel Connect Notion integration in Notion.
# NOTION_PARENT_PAGE_ID=

# AI Gateway (Vercel OIDC after `eve link` + `vercel env pull`, or set AI_GATEWAY_API_KEY)
# AI_GATEWAY_API_KEY=

# Research topic for daily schedule runs
RESEARCH_TOPIC=

# Notion Internal Integration (app-scoped; required for scheduled runs)
NOTION_API_KEY=
# Parent page UUID — integration must have access to this page
NOTION_PARENT_PAGE_ID=
